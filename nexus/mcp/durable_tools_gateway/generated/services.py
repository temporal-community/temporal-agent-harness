# Generated by nex-gen. DO NOT EDIT!

from __future__ import annotations

from nexusrpc import Operation, service
from .models import (
    CallToolInput,
    CallToolOutput,
    DeregisterInput,
    ListToolsOutput,
    RegisterExternalInput,
)


@service
class RegistryService:
    """The Durable Tools Gateway's Nexus surface. Its sole purpose is registering 3rd-party
    (non-Nexus) MCP servers and invoking their tools via a standalone activity on the
    caller's behalf — Nexus-native MCP servers register and are called directly,
    bypassing this service entirely. Service workers in any Temporal namespace call
    these operations to reach the gateway's ToolRegistryWorkflow — a Nexus call rather
    than a direct workflow signal/query so it works across namespace boundaries.
    """
    register_external: Operation[
        RegisterExternalInput,
        None,
    ] = Operation(name="RegisterExternal")
    """Register a 3rd-party external MCP server with the gateway."""

    deregister: Operation[
        DeregisterInput,
        None,
    ] = Operation(name="Deregister")
    """Remove a service registration from the gateway."""

    list_tools: Operation[
        None,
        ListToolsOutput,
    ] = Operation(name="list_tools")
    """Return all tool dicts for 3rd-party MCP servers currently registered with the
    gateway. Part of the shared MCP-over-Nexus list_tools contract (see
    mcp_over_nexus.MCPOverNexusService) so callers can treat the gateway uniformly
    alongside any Nexus-native MCP server.
    """

    call_tool: Operation[
        CallToolInput,
        CallToolOutput,
    ] = Operation(name="call_tool")
    """Invoke one tool on a 3rd-party MCP server registered with the gateway, dispatched
    via a standalone activity (no intermediary per-call workflow — the caller is itself
    already a durable Temporal workflow, so nothing here needs its own separate
    durability).
    """
