# Generated by nex-gen. DO NOT EDIT!

from __future__ import annotations

from nexusrpc import Operation, service
from .models import (
    DeregisterInput,
    RegisterExternalInput,
    RegisterNexusInput,
)


@service
class RegistryService:
    """Cross-namespace gateway for registering and deregistering MCP services. Service
    workers in any Temporal namespace call these operations to register with the
    gateway's ToolRegistryWorkflow — a Nexus call rather than a direct workflow signal
    so it works across namespace boundaries.
    """
    register_nexus: Operation[
        RegisterNexusInput,
        None,
    ] = Operation(name="RegisterNexus")
    """Register a 1st-party Nexus service with the gateway."""

    register_external: Operation[
        RegisterExternalInput,
        None,
    ] = Operation(name="RegisterExternal")
    """Register a 3rd-party external MCP server with the gateway."""

    deregister: Operation[
        DeregisterInput,
        None,
    ] = Operation(name="Deregister")
    """Remove a service registration from the gateway."""
