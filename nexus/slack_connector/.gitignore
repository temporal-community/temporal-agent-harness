# Stray local build binaries. The Makefile builds Lambda artifacts into build/
# (covered by the root .gitignore); these anchored entries catch a manual
# `go build -o webhook-lambda ./cmd/webhook-lambda` at the module root without
# ignoring the cmd/webhook-lambda and cmd/worker-lambda source directories.
/webhook-lambda
/worker-lambda
