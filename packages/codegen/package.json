{
  "name": "@temporal-agent-harness/codegen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript types for temporal-agent-harness: generated from an agent's `temporal-agent-harness schema` document, and for the event protocol every agent publishes.",
  "bin": {
    "harness-codegen": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js",
    "generate:protocol": "npm run build && uv run temporal-agent-harness schema --protocol | node dist/src/cli.js --protocol - -o src/protocol.ts"
  },
  "dependencies": {
    "json-schema-to-typescript": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
