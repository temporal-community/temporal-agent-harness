# Link @temporal-agent-harness/client from ../client rather than copying it, so this package
# always builds against the working tree (and its dist/, which a copy would leave out).
install-links=false
