# Tests for the Monty dynamic-workflow agent package. A package (not a bare dir) so the
# test modules' relative imports (e.g. ``from ..workflow import ...``) and the workflow-
# sandbox re-import of the test-only ``_subagent_e2e_parent`` resolve under the parent package.
