# Generated by nexgen v0.2.2. DO NOT EDIT!

from __future__ import annotations

import dataclasses
import typing
import typing_extensions
import temporalio.converter

from ._definitions import (
    ValidationError,
    Violation,
    _check_unique_items,
    _collect,
    _parse_spec_integer,
    _transfer_type_convertible,
)


class _AcceptedFunctionTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["AcceptedFunction", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["AcceptedFunction"]
    ) -> "AcceptedFunction":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        name_value: str = typing.cast("typing.Any", None)
        if "name" not in raw or raw["name"] is None:
            violations.append(Violation(path="name", reason="required"))
        else:
            name_value_raw = raw["name"]
            if not isinstance(name_value_raw, str):
                violations.append(Violation(path="name", reason="expected string"))
            else:
                name_value = name_value_raw

        description_value: str = typing.cast("typing.Any", None)
        if "description" not in raw or raw["description"] is None:
            violations.append(Violation(path="description", reason="required"))
        else:
            description_value_raw = raw["description"]
            if not isinstance(description_value_raw, str):
                violations.append(Violation(path="description", reason="expected string"))
            else:
                description_value = description_value_raw

        parameters_value: str = typing.cast("typing.Any", None)
        if "parameters" not in raw or raw["parameters"] is None:
            violations.append(Violation(path="parameters", reason="required"))
        else:
            parameters_value_raw = raw["parameters"]
            if not isinstance(parameters_value_raw, str):
                violations.append(Violation(path="parameters", reason="expected string"))
            else:
                parameters_value = parameters_value_raw

        output_value: str = typing.cast("typing.Any", None)
        if "output" not in raw or raw["output"] is None:
            violations.append(Violation(path="output", reason="required"))
        else:
            output_value_raw = raw["output"]
            if not isinstance(output_value_raw, str):
                violations.append(Violation(path="output", reason="expected string"))
            else:
                output_value = output_value_raw

        for key in raw:
            if key != "name" and key != "description" and key != "parameters" and key != "output":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return AcceptedFunction(
            name=name_value,
            description=description_value,
            parameters=parameters_value,
            output=output_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "AcceptedFunction") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["name"] = value.name
        out["description"] = value.description
        out["parameters"] = value.parameters
        out["output"] = value.output
        return out


@_transfer_type_convertible(_AcceptedFunctionTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class AcceptedFunction:
    """One @agent.accepts handler exposed by the agent, described tool-style (mirrors
    harness AcceptedFunction)
    """

    name: str
    """Handler name - the value to pass as msgType in sendAgentMessage"""

    description: str
    """Handler docstring describing when and how to use it"""

    parameters: str
    """JSON-encoded JSON Schema for the handler's input model"""

    output: str
    """JSON-encoded JSON Schema for the handler's return model"""


class _AgentInterfaceOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["AgentInterfaceOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["AgentInterfaceOutput"]
    ) -> "AgentInterfaceOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        handlers_value: list[AcceptedFunction] = typing.cast("typing.Any", None)
        if "handlers" not in raw or raw["handlers"] is None:
            violations.append(Violation(path="handlers", reason="required"))
        else:
            handlers_value_raw = raw["handlers"]
            if not isinstance(handlers_value_raw, list):
                violations.append(Violation(path="handlers", reason="expected array"))
            else:
                handlers_value_list: list[AcceptedFunction] = []
                for handlers_value_index, handlers_value_element in enumerate(typing.cast("list[typing.Any]", handlers_value_raw)):
                    handlers_value_item_path = f'handlers[{handlers_value_index}]'
                    handlers_value_item_violation_count = len(violations)
                    handlers_value_item: AcceptedFunction = typing.cast("typing.Any", None)
                    try:
                        handlers_value_item = _AcceptedFunctionTransferTypeConverter().from_transfer_type(handlers_value_element, AcceptedFunction)
                    except ValidationError as error:
                        _collect(violations, handlers_value_item_path, error)
                    if len(violations) == handlers_value_item_violation_count:
                        handlers_value_list.append(handlers_value_item)
                handlers_value = handlers_value_list

        for key in raw:
            if key != "handlers":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return AgentInterfaceOutput(
            handlers=handlers_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "AgentInterfaceOutput") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        handlers_out: list[typing.Any] = []
        for handlers_index, handlers_element in enumerate(value.handlers):
            try:
                handlers_out.append(_AcceptedFunctionTransferTypeConverter().to_transfer_type(handlers_element))
            except ValidationError as error:
                _collect(violations, f'handlers[{handlers_index}]', error)
        out["handlers"] = handlers_out
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_AgentInterfaceOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class AgentInterfaceOutput:

    handlers: list[AcceptedFunction]
    """Ordered list of @agent.accepts handlers the agent exposes"""


class _AgentStatusOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["AgentStatusOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["AgentStatusOutput"]
    ) -> "AgentStatusOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        agent_id_value: str = typing.cast("typing.Any", None)
        if "agentId" not in raw or raw["agentId"] is None:
            violations.append(Violation(path="agentId", reason="required"))
        else:
            agent_id_value_raw = raw["agentId"]
            if not isinstance(agent_id_value_raw, str):
                violations.append(Violation(path="agentId", reason="expected string"))
            else:
                agent_id_value = agent_id_value_raw

        current_turn_value: int = typing.cast("typing.Any", None)
        if "currentTurn" not in raw or raw["currentTurn"] is None:
            violations.append(Violation(path="currentTurn", reason="required"))
        else:
            current_turn_value_raw = raw["currentTurn"]
            current_turn_value_parsed = _parse_spec_integer(current_turn_value_raw, "currentTurn", violations)
            if current_turn_value_parsed is not None:
                current_turn_value = current_turn_value_parsed

        turn_active_value: bool = typing.cast("typing.Any", None)
        if "turnActive" not in raw or raw["turnActive"] is None:
            violations.append(Violation(path="turnActive", reason="required"))
        else:
            turn_active_value_raw = raw["turnActive"]
            if not isinstance(turn_active_value_raw, bool):
                violations.append(Violation(path="turnActive", reason="expected boolean"))
            else:
                turn_active_value = turn_active_value_raw

        is_message_queuing_enabled_value: bool = typing.cast("typing.Any", None)
        if "isMessageQueuingEnabled" not in raw or raw["isMessageQueuingEnabled"] is None:
            violations.append(Violation(path="isMessageQueuingEnabled", reason="required"))
        else:
            is_message_queuing_enabled_value_raw = raw["isMessageQueuingEnabled"]
            if not isinstance(is_message_queuing_enabled_value_raw, bool):
                violations.append(Violation(path="isMessageQueuingEnabled", reason="expected boolean"))
            else:
                is_message_queuing_enabled_value = is_message_queuing_enabled_value_raw

        pending_turns_value: list[PendingTurn] = typing.cast("typing.Any", None)
        if "pendingTurns" not in raw or raw["pendingTurns"] is None:
            violations.append(Violation(path="pendingTurns", reason="required"))
        else:
            pending_turns_value_raw = raw["pendingTurns"]
            if not isinstance(pending_turns_value_raw, list):
                violations.append(Violation(path="pendingTurns", reason="expected array"))
            else:
                pending_turns_value_list: list[PendingTurn] = []
                for pending_turns_value_index, pending_turns_value_element in enumerate(typing.cast("list[typing.Any]", pending_turns_value_raw)):
                    pending_turns_value_item_path = f'pendingTurns[{pending_turns_value_index}]'
                    pending_turns_value_item_violation_count = len(violations)
                    pending_turns_value_item: PendingTurn = typing.cast("typing.Any", None)
                    try:
                        pending_turns_value_item = _PendingTurnTransferTypeConverter().from_transfer_type(pending_turns_value_element, PendingTurn)
                    except ValidationError as error:
                        _collect(violations, pending_turns_value_item_path, error)
                    if len(violations) == pending_turns_value_item_violation_count:
                        pending_turns_value_list.append(pending_turns_value_item)
                pending_turns_value = pending_turns_value_list

        pending_approvals_value: list[PendingApproval] = typing.cast("typing.Any", None)
        if "pendingApprovals" not in raw or raw["pendingApprovals"] is None:
            violations.append(Violation(path="pendingApprovals", reason="required"))
        else:
            pending_approvals_value_raw = raw["pendingApprovals"]
            if not isinstance(pending_approvals_value_raw, list):
                violations.append(Violation(path="pendingApprovals", reason="expected array"))
            else:
                pending_approvals_value_list: list[PendingApproval] = []
                for pending_approvals_value_index, pending_approvals_value_element in enumerate(typing.cast("list[typing.Any]", pending_approvals_value_raw)):
                    pending_approvals_value_item_path = f'pendingApprovals[{pending_approvals_value_index}]'
                    pending_approvals_value_item_violation_count = len(violations)
                    pending_approvals_value_item: PendingApproval = typing.cast("typing.Any", None)
                    try:
                        pending_approvals_value_item = _PendingApprovalTransferTypeConverter().from_transfer_type(pending_approvals_value_element, PendingApproval)
                    except ValidationError as error:
                        _collect(violations, pending_approvals_value_item_path, error)
                    if len(violations) == pending_approvals_value_item_violation_count:
                        pending_approvals_value_list.append(pending_approvals_value_item)
                pending_approvals_value = pending_approvals_value_list

        pending_callbacks_value: list[PendingCallback] = typing.cast("typing.Any", None)
        if "pendingCallbacks" not in raw or raw["pendingCallbacks"] is None:
            violations.append(Violation(path="pendingCallbacks", reason="required"))
        else:
            pending_callbacks_value_raw = raw["pendingCallbacks"]
            if not isinstance(pending_callbacks_value_raw, list):
                violations.append(Violation(path="pendingCallbacks", reason="expected array"))
            else:
                pending_callbacks_value_list: list[PendingCallback] = []
                for pending_callbacks_value_index, pending_callbacks_value_element in enumerate(typing.cast("list[typing.Any]", pending_callbacks_value_raw)):
                    pending_callbacks_value_item_path = f'pendingCallbacks[{pending_callbacks_value_index}]'
                    pending_callbacks_value_item_violation_count = len(violations)
                    pending_callbacks_value_item: PendingCallback = typing.cast("typing.Any", None)
                    try:
                        pending_callbacks_value_item = _PendingCallbackTransferTypeConverter().from_transfer_type(pending_callbacks_value_element, PendingCallback)
                    except ValidationError as error:
                        _collect(violations, pending_callbacks_value_item_path, error)
                    if len(violations) == pending_callbacks_value_item_violation_count:
                        pending_callbacks_value_list.append(pending_callbacks_value_item)
                pending_callbacks_value = pending_callbacks_value_list

        subagents_value: list[SubagentInfo] = typing.cast("typing.Any", None)
        if "subagents" not in raw or raw["subagents"] is None:
            violations.append(Violation(path="subagents", reason="required"))
        else:
            subagents_value_raw = raw["subagents"]
            if not isinstance(subagents_value_raw, list):
                violations.append(Violation(path="subagents", reason="expected array"))
            else:
                subagents_value_list: list[SubagentInfo] = []
                for subagents_value_index, subagents_value_element in enumerate(typing.cast("list[typing.Any]", subagents_value_raw)):
                    subagents_value_item_path = f'subagents[{subagents_value_index}]'
                    subagents_value_item_violation_count = len(violations)
                    subagents_value_item: SubagentInfo = typing.cast("typing.Any", None)
                    try:
                        subagents_value_item = _SubagentInfoTransferTypeConverter().from_transfer_type(subagents_value_element, SubagentInfo)
                    except ValidationError as error:
                        _collect(violations, subagents_value_item_path, error)
                    if len(violations) == subagents_value_item_violation_count:
                        subagents_value_list.append(subagents_value_item)
                subagents_value = subagents_value_list

        approval_policy_value: ApprovalPolicy = typing.cast("typing.Any", None)
        if "approvalPolicy" not in raw or raw["approvalPolicy"] is None:
            violations.append(Violation(path="approvalPolicy", reason="required"))
        else:
            approval_policy_value_raw = raw["approvalPolicy"]
            try:
                approval_policy_value = _ApprovalPolicyTransferTypeConverter().from_transfer_type(approval_policy_value_raw, ApprovalPolicy)
            except ValidationError as error:
                _collect(violations, "approvalPolicy", error)

        has_custom_approval_fallback_value: bool = typing.cast("typing.Any", None)
        if "hasCustomApprovalFallback" not in raw or raw["hasCustomApprovalFallback"] is None:
            violations.append(Violation(path="hasCustomApprovalFallback", reason="required"))
        else:
            has_custom_approval_fallback_value_raw = raw["hasCustomApprovalFallback"]
            if not isinstance(has_custom_approval_fallback_value_raw, bool):
                violations.append(Violation(path="hasCustomApprovalFallback", reason="expected boolean"))
            else:
                has_custom_approval_fallback_value = has_custom_approval_fallback_value_raw

        for key in raw:
            if key != "agentId" and key != "currentTurn" and key != "turnActive" and key != "isMessageQueuingEnabled" and key != "pendingTurns" and key != "pendingApprovals" and key != "pendingCallbacks" and key != "subagents" and key != "approvalPolicy" and key != "hasCustomApprovalFallback":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return AgentStatusOutput(
            agent_id=agent_id_value,
            current_turn=current_turn_value,
            turn_active=turn_active_value,
            is_message_queuing_enabled=is_message_queuing_enabled_value,
            pending_turns=pending_turns_value,
            pending_approvals=pending_approvals_value,
            pending_callbacks=pending_callbacks_value,
            subagents=subagents_value,
            approval_policy=approval_policy_value,
            has_custom_approval_fallback=has_custom_approval_fallback_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "AgentStatusOutput") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        out["agentId"] = value.agent_id
        out["currentTurn"] = value.current_turn
        out["turnActive"] = value.turn_active
        out["isMessageQueuingEnabled"] = value.is_message_queuing_enabled
        pending_turns_out: list[typing.Any] = []
        for pending_turns_index, pending_turns_element in enumerate(value.pending_turns):
            try:
                pending_turns_out.append(_PendingTurnTransferTypeConverter().to_transfer_type(pending_turns_element))
            except ValidationError as error:
                _collect(violations, f'pendingTurns[{pending_turns_index}]', error)
        out["pendingTurns"] = pending_turns_out
        pending_approvals_out: list[typing.Any] = []
        for pending_approvals_index, pending_approvals_element in enumerate(value.pending_approvals):
            try:
                pending_approvals_out.append(_PendingApprovalTransferTypeConverter().to_transfer_type(pending_approvals_element))
            except ValidationError as error:
                _collect(violations, f'pendingApprovals[{pending_approvals_index}]', error)
        out["pendingApprovals"] = pending_approvals_out
        pending_callbacks_out: list[typing.Any] = []
        for pending_callbacks_index, pending_callbacks_element in enumerate(value.pending_callbacks):
            try:
                pending_callbacks_out.append(_PendingCallbackTransferTypeConverter().to_transfer_type(pending_callbacks_element))
            except ValidationError as error:
                _collect(violations, f'pendingCallbacks[{pending_callbacks_index}]', error)
        out["pendingCallbacks"] = pending_callbacks_out
        subagents_out: list[typing.Any] = []
        for subagents_index, subagents_element in enumerate(value.subagents):
            try:
                subagents_out.append(_SubagentInfoTransferTypeConverter().to_transfer_type(subagents_element))
            except ValidationError as error:
                _collect(violations, f'subagents[{subagents_index}]', error)
        out["subagents"] = subagents_out
        try:
            out["approvalPolicy"] = _ApprovalPolicyTransferTypeConverter().to_transfer_type(value.approval_policy)
        except ValidationError as error:
            _collect(violations, "approvalPolicy", error)
        out["hasCustomApprovalFallback"] = value.has_custom_approval_fallback
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_AgentStatusOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class AgentStatusOutput:
    """Mirrors harness AgentStatus - the single source of truth for session state"""

    agent_id: str
    """This agent's own short ID (stamped on every event it publishes)"""

    current_turn: int
    """Monotonic turn counter"""

    turn_active: bool
    """True if a turn is currently running"""

    is_message_queuing_enabled: bool
    """True if the session accepts messages while a turn is active"""

    pending_turns: list[PendingTurn]
    """Messages waiting in the agent's queue behind the active turn"""

    pending_approvals: list[PendingApproval]
    """Gated tool calls currently awaiting a human decision"""

    pending_callbacks: list[PendingCallback]
    """Callback tool calls currently awaiting a client-supplied result"""

    subagents: list[SubagentInfo]
    """Subagents this agent is currently driving"""

    approval_policy: ApprovalPolicy

    has_custom_approval_fallback: bool
    """Whether a developer custom-approval predicate is wired (the predicate itself is
    non-serializable and never surfaced)
    """


class _ApprovalPolicyTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ApprovalPolicy", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ApprovalPolicy"]
    ) -> "ApprovalPolicy":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        dangerously_skip_all_approvals_value: bool = typing.cast("typing.Any", None)
        if "dangerouslySkipAllApprovals" not in raw or raw["dangerouslySkipAllApprovals"] is None:
            violations.append(Violation(path="dangerouslySkipAllApprovals", reason="required"))
        else:
            dangerously_skip_all_approvals_value_raw = raw["dangerouslySkipAllApprovals"]
            if not isinstance(dangerously_skip_all_approvals_value_raw, bool):
                violations.append(Violation(path="dangerouslySkipAllApprovals", reason="expected boolean"))
            else:
                dangerously_skip_all_approvals_value = dangerously_skip_all_approvals_value_raw

        auto_approve_inherently_safe_value: bool = typing.cast("typing.Any", None)
        if "autoApproveInherentlySafe" not in raw or raw["autoApproveInherentlySafe"] is None:
            violations.append(Violation(path="autoApproveInherentlySafe", reason="required"))
        else:
            auto_approve_inherently_safe_value_raw = raw["autoApproveInherentlySafe"]
            if not isinstance(auto_approve_inherently_safe_value_raw, bool):
                violations.append(Violation(path="autoApproveInherentlySafe", reason="expected boolean"))
            else:
                auto_approve_inherently_safe_value = auto_approve_inherently_safe_value_raw

        auto_approve_tools_value: list[str] = typing.cast("typing.Any", None)
        if "autoApproveTools" not in raw or raw["autoApproveTools"] is None:
            violations.append(Violation(path="autoApproveTools", reason="required"))
        else:
            auto_approve_tools_value_raw = raw["autoApproveTools"]
            if not isinstance(auto_approve_tools_value_raw, list):
                violations.append(Violation(path="autoApproveTools", reason="expected array"))
            else:
                auto_approve_tools_value_list: list[str] = []
                for auto_approve_tools_value_index, auto_approve_tools_value_element in enumerate(typing.cast("list[typing.Any]", auto_approve_tools_value_raw)):
                    auto_approve_tools_value_item_path = f'autoApproveTools[{auto_approve_tools_value_index}]'
                    auto_approve_tools_value_item_violation_count = len(violations)
                    auto_approve_tools_value_item: str = typing.cast("typing.Any", None)
                    if not isinstance(auto_approve_tools_value_element, str):
                        violations.append(Violation(path=auto_approve_tools_value_item_path, reason="expected string"))
                    else:
                        auto_approve_tools_value_item = auto_approve_tools_value_element
                    if len(violations) == auto_approve_tools_value_item_violation_count:
                        auto_approve_tools_value_list.append(auto_approve_tools_value_item)
                _check_unique_items(typing.cast("list[typing.Any]", auto_approve_tools_value_raw), "autoApproveTools", violations)
                auto_approve_tools_value = auto_approve_tools_value_list

        for key in raw:
            if key != "dangerouslySkipAllApprovals" and key != "autoApproveInherentlySafe" and key != "autoApproveTools":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ApprovalPolicy(
            dangerously_skip_all_approvals=dangerously_skip_all_approvals_value,
            auto_approve_inherently_safe=auto_approve_inherently_safe_value,
            auto_approve_tools=auto_approve_tools_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ApprovalPolicy") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        out["dangerouslySkipAllApprovals"] = value.dangerously_skip_all_approvals
        out["autoApproveInherentlySafe"] = value.auto_approve_inherently_safe
        _check_unique_items(value.auto_approve_tools, "autoApproveTools", violations)
        out["autoApproveTools"] = value.auto_approve_tools
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_ApprovalPolicyTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ApprovalPolicy:
    """The live tool-approval policy the agent is running under (mirrors harness
    ToolApprovalPolicy)
    """

    dangerously_skip_all_approvals: bool
    """If true, no tool call is ever gated"""

    auto_approve_inherently_safe: bool
    """If true, tools that declared themselves inherently_safe skip the gate"""

    auto_approve_tools: list[str]
    """Specific tool names auto-approved regardless of the layers above"""


class _ApproveToolCallInputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ApproveToolCallInput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ApproveToolCallInput"]
    ) -> "ApproveToolCallInput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        session_id_value: str = typing.cast("typing.Any", None)
        if "sessionId" not in raw or raw["sessionId"] is None:
            violations.append(Violation(path="sessionId", reason="required"))
        else:
            session_id_value_raw = raw["sessionId"]
            if not isinstance(session_id_value_raw, str):
                violations.append(Violation(path="sessionId", reason="expected string"))
            else:
                session_id_value = session_id_value_raw

        tool_id_value: str = typing.cast("typing.Any", None)
        if "toolId" not in raw or raw["toolId"] is None:
            violations.append(Violation(path="toolId", reason="required"))
        else:
            tool_id_value_raw = raw["toolId"]
            if not isinstance(tool_id_value_raw, str):
                violations.append(Violation(path="toolId", reason="expected string"))
            else:
                tool_id_value = tool_id_value_raw

        approved_value: bool = typing.cast("typing.Any", None)
        if "approved" not in raw or raw["approved"] is None:
            violations.append(Violation(path="approved", reason="required"))
        else:
            approved_value_raw = raw["approved"]
            if not isinstance(approved_value_raw, bool):
                violations.append(Violation(path="approved", reason="expected boolean"))
            else:
                approved_value = approved_value_raw

        reason_value: str | None = None
        if "reason" in raw:
            reason_value_raw = raw["reason"]
            if reason_value_raw is None:
                violations.append(Violation(path="reason", reason="explicit null not allowed"))
            else:
                if not isinstance(reason_value_raw, str):
                    violations.append(Violation(path="reason", reason="expected string"))
                else:
                    reason_value = reason_value_raw

        remember_value: bool | None = None
        if "remember" in raw:
            remember_value_raw = raw["remember"]
            if remember_value_raw is None:
                violations.append(Violation(path="remember", reason="explicit null not allowed"))
            else:
                if not isinstance(remember_value_raw, bool):
                    violations.append(Violation(path="remember", reason="expected boolean"))
                else:
                    remember_value = remember_value_raw

        for key in raw:
            if key != "sessionId" and key != "toolId" and key != "approved" and key != "reason" and key != "remember":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ApproveToolCallInput(
            session_id=session_id_value,
            tool_id=tool_id_value,
            approved=approved_value,
            reason=reason_value,
            remember=remember_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ApproveToolCallInput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["sessionId"] = value.session_id
        out["toolId"] = value.tool_id
        out["approved"] = value.approved
        if value.reason is not None:
            out["reason"] = value.reason
        if value.remember is not None:
            out["remember"] = value.remember
        return out


@_transfer_type_convertible(_ApproveToolCallInputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ApproveToolCallInput:

    session_id: str
    """Provider-prefixed session identifier"""

    tool_id: str
    """The tool_id from the ToolApprovalRequested stream event"""

    approved: bool
    """True to allow the tool call, false to deny it"""

    reason: str | None = None
    """Optional note surfaced on the resolution event and, on denial, fed back to the model"""

    remember: bool | None = None
    """If true and approved, adds the tool to the session's auto-approve list so it won't
    be gated again
    """


class _ApproveToolCallOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ApproveToolCallOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ApproveToolCallOutput"]
    ) -> "ApproveToolCallOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        tool_id_value: str = typing.cast("typing.Any", None)
        if "toolId" not in raw or raw["toolId"] is None:
            violations.append(Violation(path="toolId", reason="required"))
        else:
            tool_id_value_raw = raw["toolId"]
            if not isinstance(tool_id_value_raw, str):
                violations.append(Violation(path="toolId", reason="expected string"))
            else:
                tool_id_value = tool_id_value_raw

        accepted_value: bool = typing.cast("typing.Any", None)
        if "accepted" not in raw or raw["accepted"] is None:
            violations.append(Violation(path="accepted", reason="required"))
        else:
            accepted_value_raw = raw["accepted"]
            if not isinstance(accepted_value_raw, bool):
                violations.append(Violation(path="accepted", reason="expected boolean"))
            else:
                accepted_value = accepted_value_raw

        for key in raw:
            if key != "toolId" and key != "accepted":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ApproveToolCallOutput(
            tool_id=tool_id_value,
            accepted=accepted_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ApproveToolCallOutput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["toolId"] = value.tool_id
        out["accepted"] = value.accepted
        return out


@_transfer_type_convertible(_ApproveToolCallOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ApproveToolCallOutput:

    tool_id: str
    """Echo of the resolved tool ID"""

    accepted: bool
    """Always true on success (the update validator rejects invalid decisions before the
    handler runs)
    """


class _CloseSessionOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["CloseSessionOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["CloseSessionOutput"]
    ) -> "CloseSessionOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        closed_value: bool = typing.cast("typing.Any", None)
        if "closed" not in raw or raw["closed"] is None:
            violations.append(Violation(path="closed", reason="required"))
        else:
            closed_value_raw = raw["closed"]
            if not isinstance(closed_value_raw, bool):
                violations.append(Violation(path="closed", reason="expected boolean"))
            else:
                closed_value = closed_value_raw

        for key in raw:
            if key != "closed":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return CloseSessionOutput(
            closed=closed_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "CloseSessionOutput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["closed"] = value.closed
        return out


@_transfer_type_convertible(_CloseSessionOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class CloseSessionOutput:

    closed: bool
    """Always true on success"""


class _ExecuteOperatorCommandInputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ExecuteOperatorCommandInput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ExecuteOperatorCommandInput"]
    ) -> "ExecuteOperatorCommandInput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        session_id_value: str = typing.cast("typing.Any", None)
        if "sessionId" not in raw or raw["sessionId"] is None:
            violations.append(Violation(path="sessionId", reason="required"))
        else:
            session_id_value_raw = raw["sessionId"]
            if not isinstance(session_id_value_raw, str):
                violations.append(Violation(path="sessionId", reason="expected string"))
            else:
                session_id_value = session_id_value_raw

        name_value: str = typing.cast("typing.Any", None)
        if "name" not in raw or raw["name"] is None:
            violations.append(Violation(path="name", reason="required"))
        else:
            name_value_raw = raw["name"]
            if not isinstance(name_value_raw, str):
                violations.append(Violation(path="name", reason="expected string"))
            else:
                name_value = name_value_raw

        arg_value: str | None = None
        if "arg" in raw:
            arg_value_raw = raw["arg"]
            if arg_value_raw is None:
                violations.append(Violation(path="arg", reason="explicit null not allowed"))
            else:
                if not isinstance(arg_value_raw, str):
                    violations.append(Violation(path="arg", reason="expected string"))
                else:
                    arg_value = arg_value_raw

        for key in raw:
            if key != "sessionId" and key != "name" and key != "arg":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ExecuteOperatorCommandInput(
            session_id=session_id_value,
            name=name_value,
            arg=arg_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ExecuteOperatorCommandInput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["sessionId"] = value.session_id
        out["name"] = value.name
        if value.arg is not None:
            out["arg"] = value.arg
        return out


@_transfer_type_convertible(_ExecuteOperatorCommandInputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ExecuteOperatorCommandInput:

    session_id: str
    """Provider-prefixed session identifier"""

    name: str
    """Operator command name (e.g. 'approvals', 'stop', 'status')"""

    arg: str | None = None
    """Optional argument string"""


class _ExecuteOperatorCommandOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ExecuteOperatorCommandOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ExecuteOperatorCommandOutput"]
    ) -> "ExecuteOperatorCommandOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        reply_value: str = typing.cast("typing.Any", None)
        if "reply" not in raw or raw["reply"] is None:
            violations.append(Violation(path="reply", reason="required"))
        else:
            reply_value_raw = raw["reply"]
            if not isinstance(reply_value_raw, str):
                violations.append(Violation(path="reply", reason="expected string"))
            else:
                reply_value = reply_value_raw

        for key in raw:
            if key != "reply":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ExecuteOperatorCommandOutput(
            reply=reply_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ExecuteOperatorCommandOutput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["reply"] = value.reply
        return out


@_transfer_type_convertible(_ExecuteOperatorCommandOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ExecuteOperatorCommandOutput:

    reply: str
    """Human-readable reply text from the harness"""


class _OperatorCommandTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["OperatorCommand", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["OperatorCommand"]
    ) -> "OperatorCommand":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        name_value: str = typing.cast("typing.Any", None)
        if "name" not in raw or raw["name"] is None:
            violations.append(Violation(path="name", reason="required"))
        else:
            name_value_raw = raw["name"]
            if not isinstance(name_value_raw, str):
                violations.append(Violation(path="name", reason="expected string"))
            else:
                name_value = name_value_raw

        label_value: str = typing.cast("typing.Any", None)
        if "label" not in raw or raw["label"] is None:
            violations.append(Violation(path="label", reason="required"))
        else:
            label_value_raw = raw["label"]
            if not isinstance(label_value_raw, str):
                violations.append(Violation(path="label", reason="expected string"))
            else:
                label_value = label_value_raw

        description_value: str = typing.cast("typing.Any", None)
        if "description" not in raw or raw["description"] is None:
            violations.append(Violation(path="description", reason="required"))
        else:
            description_value_raw = raw["description"]
            if not isinstance(description_value_raw, str):
                violations.append(Violation(path="description", reason="expected string"))
            else:
                description_value = description_value_raw

        source_value: str = typing.cast("typing.Any", None)
        if "source" not in raw or raw["source"] is None:
            violations.append(Violation(path="source", reason="required"))
        else:
            source_value_raw = raw["source"]
            if not isinstance(source_value_raw, str):
                violations.append(Violation(path="source", reason="expected string"))
            else:
                source_value = source_value_raw

        argument_value: OperatorCommandArgument | None = None
        if "argument" in raw:
            argument_value_raw = raw["argument"]
            if argument_value_raw is None:
                violations.append(Violation(path="argument", reason="explicit null not allowed"))
            else:
                try:
                    argument_value = _OperatorCommandArgumentTransferTypeConverter().from_transfer_type(argument_value_raw, OperatorCommandArgument)
                except ValidationError as error:
                    _collect(violations, "argument", error)

        for key in raw:
            if key != "name" and key != "label" and key != "description" and key != "source" and key != "argument":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return OperatorCommand(
            name=name_value,
            label=label_value,
            description=description_value,
            source=source_value,
            argument=argument_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "OperatorCommand") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        out["name"] = value.name
        out["label"] = value.label
        out["description"] = value.description
        out["source"] = value.source
        if value.argument is not None:
            try:
                out["argument"] = _OperatorCommandArgumentTransferTypeConverter().to_transfer_type(value.argument)
            except ValidationError as error:
                _collect(violations, "argument", error)
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_OperatorCommandTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class OperatorCommand:

    name: str
    """Canonical command name used as the routing key (e.g. 'scope')"""

    label: str
    """Display label including the slash (e.g. '/scope')"""

    description: str
    """Human-readable description suitable for a Slack manifest entry"""

    source: str
    """'harness' for built-in commands, 'agent' for agent-registered ones"""

    argument: OperatorCommandArgument | None = None


class _OperatorCommandArgumentTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["OperatorCommandArgument", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["OperatorCommandArgument"]
    ) -> "OperatorCommandArgument":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        kind_value: str = typing.cast("typing.Any", None)
        if "kind" not in raw or raw["kind"] is None:
            violations.append(Violation(path="kind", reason="required"))
        else:
            kind_value_raw = raw["kind"]
            if not isinstance(kind_value_raw, str):
                violations.append(Violation(path="kind", reason="expected string"))
            else:
                kind_value = kind_value_raw

        required_value: bool = typing.cast("typing.Any", None)
        if "required" not in raw or raw["required"] is None:
            violations.append(Violation(path="required", reason="required"))
        else:
            required_value_raw = raw["required"]
            if not isinstance(required_value_raw, bool):
                violations.append(Violation(path="required", reason="expected boolean"))
            else:
                required_value = required_value_raw

        choices_value: list[str] | None = None
        if "choices" in raw:
            choices_value_raw = raw["choices"]
            if choices_value_raw is None:
                violations.append(Violation(path="choices", reason="explicit null not allowed"))
            else:
                if not isinstance(choices_value_raw, list):
                    violations.append(Violation(path="choices", reason="expected array"))
                else:
                    choices_value_list: list[str] = []
                    for choices_value_index, choices_value_element in enumerate(typing.cast("list[typing.Any]", choices_value_raw)):
                        choices_value_item_path = f'choices[{choices_value_index}]'
                        choices_value_item_violation_count = len(violations)
                        choices_value_item: str = typing.cast("typing.Any", None)
                        if not isinstance(choices_value_element, str):
                            violations.append(Violation(path=choices_value_item_path, reason="expected string"))
                        else:
                            choices_value_item = choices_value_element
                        if len(violations) == choices_value_item_violation_count:
                            choices_value_list.append(choices_value_item)
                    choices_value = choices_value_list

        placeholder_value: str | None = None
        if "placeholder" in raw:
            placeholder_value_raw = raw["placeholder"]
            if placeholder_value_raw is None:
                violations.append(Violation(path="placeholder", reason="explicit null not allowed"))
            else:
                if not isinstance(placeholder_value_raw, str):
                    violations.append(Violation(path="placeholder", reason="expected string"))
                else:
                    placeholder_value = placeholder_value_raw

        allow_multiple_value: bool | None = None
        if "allowMultiple" in raw:
            allow_multiple_value_raw = raw["allowMultiple"]
            if allow_multiple_value_raw is None:
                violations.append(Violation(path="allowMultiple", reason="explicit null not allowed"))
            else:
                if not isinstance(allow_multiple_value_raw, bool):
                    violations.append(Violation(path="allowMultiple", reason="expected boolean"))
                else:
                    allow_multiple_value = allow_multiple_value_raw

        for key in raw:
            if key != "kind" and key != "required" and key != "choices" and key != "placeholder" and key != "allowMultiple":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return OperatorCommandArgument(
            kind=kind_value,
            required=required_value,
            choices=choices_value,
            placeholder=placeholder_value,
            allow_multiple=allow_multiple_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "OperatorCommandArgument") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["kind"] = value.kind
        out["required"] = value.required
        if value.choices is not None:
            out["choices"] = value.choices
        if value.placeholder is not None:
            out["placeholder"] = value.placeholder
        if value.allow_multiple is not None:
            out["allowMultiple"] = value.allow_multiple
        return out


@_transfer_type_convertible(_OperatorCommandArgumentTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class OperatorCommandArgument:
    """Argument metadata for rendering a UI hint or menu"""

    kind: str
    """'enum' | 'text' | 'tool_names'"""

    required: bool
    """Whether the argument must be provided"""

    choices: list[str] | None = None
    """Valid choices when kind is 'enum'"""

    placeholder: str | None = None
    """Hint text for the argument (e.g. 'docs|forum|all')"""

    allow_multiple: bool | None = None
    """Whether multiple space-separated values are accepted"""


class _PendingApprovalTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["PendingApproval", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["PendingApproval"]
    ) -> "PendingApproval":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        tool_id_value: str = typing.cast("typing.Any", None)
        if "toolId" not in raw or raw["toolId"] is None:
            violations.append(Violation(path="toolId", reason="required"))
        else:
            tool_id_value_raw = raw["toolId"]
            if not isinstance(tool_id_value_raw, str):
                violations.append(Violation(path="toolId", reason="expected string"))
            else:
                tool_id_value = tool_id_value_raw

        tool_name_value: str = typing.cast("typing.Any", None)
        if "toolName" not in raw or raw["toolName"] is None:
            violations.append(Violation(path="toolName", reason="required"))
        else:
            tool_name_value_raw = raw["toolName"]
            if not isinstance(tool_name_value_raw, str):
                violations.append(Violation(path="toolName", reason="expected string"))
            else:
                tool_name_value = tool_name_value_raw

        tool_input_value: str = typing.cast("typing.Any", None)
        if "toolInput" not in raw or raw["toolInput"] is None:
            violations.append(Violation(path="toolInput", reason="required"))
        else:
            tool_input_value_raw = raw["toolInput"]
            if not isinstance(tool_input_value_raw, str):
                violations.append(Violation(path="toolInput", reason="expected string"))
            else:
                tool_input_value = tool_input_value_raw

        turn_number_value: int = typing.cast("typing.Any", None)
        if "turnNumber" not in raw or raw["turnNumber"] is None:
            violations.append(Violation(path="turnNumber", reason="required"))
        else:
            turn_number_value_raw = raw["turnNumber"]
            turn_number_value_parsed = _parse_spec_integer(turn_number_value_raw, "turnNumber", violations)
            if turn_number_value_parsed is not None:
                turn_number_value = turn_number_value_parsed

        for key in raw:
            if key != "toolId" and key != "toolName" and key != "toolInput" and key != "turnNumber":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return PendingApproval(
            tool_id=tool_id_value,
            tool_name=tool_name_value,
            tool_input=tool_input_value,
            turn_number=turn_number_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "PendingApproval") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["toolId"] = value.tool_id
        out["toolName"] = value.tool_name
        out["toolInput"] = value.tool_input
        out["turnNumber"] = value.turn_number
        return out


@_transfer_type_convertible(_PendingApprovalTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class PendingApproval:
    """A gated tool call awaiting a human decision (mirrors harness PendingApproval)"""

    tool_id: str
    """ID to pass to approveToolCall"""

    tool_name: str
    """Human-readable tool name"""

    tool_input: str
    """JSON-encoded model-facing tool input (injected parameters excluded)"""

    turn_number: int
    """Turn that triggered this approval request"""


class _PendingCallbackTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["PendingCallback", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["PendingCallback"]
    ) -> "PendingCallback":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        tool_id_value: str = typing.cast("typing.Any", None)
        if "toolId" not in raw or raw["toolId"] is None:
            violations.append(Violation(path="toolId", reason="required"))
        else:
            tool_id_value_raw = raw["toolId"]
            if not isinstance(tool_id_value_raw, str):
                violations.append(Violation(path="toolId", reason="expected string"))
            else:
                tool_id_value = tool_id_value_raw

        tool_name_value: str = typing.cast("typing.Any", None)
        if "toolName" not in raw or raw["toolName"] is None:
            violations.append(Violation(path="toolName", reason="required"))
        else:
            tool_name_value_raw = raw["toolName"]
            if not isinstance(tool_name_value_raw, str):
                violations.append(Violation(path="toolName", reason="expected string"))
            else:
                tool_name_value = tool_name_value_raw

        tool_input_value: str = typing.cast("typing.Any", None)
        if "toolInput" not in raw or raw["toolInput"] is None:
            violations.append(Violation(path="toolInput", reason="required"))
        else:
            tool_input_value_raw = raw["toolInput"]
            if not isinstance(tool_input_value_raw, str):
                violations.append(Violation(path="toolInput", reason="expected string"))
            else:
                tool_input_value = tool_input_value_raw

        output_schema_value: str = typing.cast("typing.Any", None)
        if "outputSchema" not in raw or raw["outputSchema"] is None:
            violations.append(Violation(path="outputSchema", reason="required"))
        else:
            output_schema_value_raw = raw["outputSchema"]
            if not isinstance(output_schema_value_raw, str):
                violations.append(Violation(path="outputSchema", reason="expected string"))
            else:
                output_schema_value = output_schema_value_raw

        turn_number_value: int = typing.cast("typing.Any", None)
        if "turnNumber" not in raw or raw["turnNumber"] is None:
            violations.append(Violation(path="turnNumber", reason="required"))
        else:
            turn_number_value_raw = raw["turnNumber"]
            turn_number_value_parsed = _parse_spec_integer(turn_number_value_raw, "turnNumber", violations)
            if turn_number_value_parsed is not None:
                turn_number_value = turn_number_value_parsed

        for key in raw:
            if key != "toolId" and key != "toolName" and key != "toolInput" and key != "outputSchema" and key != "turnNumber":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return PendingCallback(
            tool_id=tool_id_value,
            tool_name=tool_name_value,
            tool_input=tool_input_value,
            output_schema=output_schema_value,
            turn_number=turn_number_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "PendingCallback") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["toolId"] = value.tool_id
        out["toolName"] = value.tool_name
        out["toolInput"] = value.tool_input
        out["outputSchema"] = value.output_schema
        out["turnNumber"] = value.turn_number
        return out


@_transfer_type_convertible(_PendingCallbackTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class PendingCallback:
    """A callback tool call awaiting a client-supplied result (mirrors harness
    PendingCallback)
    """

    tool_id: str
    """ID to pass to provideCallbackResult"""

    tool_name: str
    """Human-readable tool name"""

    tool_input: str
    """JSON-encoded model-facing tool input (injected parameters excluded)"""

    output_schema: str
    """JSON-encoded JSON Schema the submitted result must match"""

    turn_number: int
    """Turn that triggered this callback request"""


class _PendingTurnTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["PendingTurn", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["PendingTurn"]
    ) -> "PendingTurn":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        turn_number_value: int = typing.cast("typing.Any", None)
        if "turnNumber" not in raw or raw["turnNumber"] is None:
            violations.append(Violation(path="turnNumber", reason="required"))
        else:
            turn_number_value_raw = raw["turnNumber"]
            turn_number_value_parsed = _parse_spec_integer(turn_number_value_raw, "turnNumber", violations)
            if turn_number_value_parsed is not None:
                turn_number_value = turn_number_value_parsed

        turn_id_value: str = typing.cast("typing.Any", None)
        if "turnId" not in raw or raw["turnId"] is None:
            violations.append(Violation(path="turnId", reason="required"))
        else:
            turn_id_value_raw = raw["turnId"]
            if not isinstance(turn_id_value_raw, str):
                violations.append(Violation(path="turnId", reason="expected string"))
            else:
                turn_id_value = turn_id_value_raw

        message_value: str = typing.cast("typing.Any", None)
        if "message" not in raw or raw["message"] is None:
            violations.append(Violation(path="message", reason="required"))
        else:
            message_value_raw = raw["message"]
            if not isinstance(message_value_raw, str):
                violations.append(Violation(path="message", reason="expected string"))
            else:
                message_value = message_value_raw

        for key in raw:
            if key != "turnNumber" and key != "turnId" and key != "message":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return PendingTurn(
            turn_number=turn_number_value,
            turn_id=turn_id_value,
            message=message_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "PendingTurn") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["turnNumber"] = value.turn_number
        out["turnId"] = value.turn_id
        out["message"] = value.message
        return out


@_transfer_type_convertible(_PendingTurnTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class PendingTurn:
    """A message waiting in the agent's queue (mirrors harness PendingTurn)"""

    turn_number: int

    turn_id: str

    message: str
    """JSON-compacted {type, payload} envelope of the queued message"""


class _PollMessagesInputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["PollMessagesInput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["PollMessagesInput"]
    ) -> "PollMessagesInput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        session_id_value: str = typing.cast("typing.Any", None)
        if "sessionId" not in raw or raw["sessionId"] is None:
            violations.append(Violation(path="sessionId", reason="required"))
        else:
            session_id_value_raw = raw["sessionId"]
            if not isinstance(session_id_value_raw, str):
                violations.append(Violation(path="sessionId", reason="expected string"))
            else:
                session_id_value = session_id_value_raw

        cursor_value: int = typing.cast("typing.Any", None)
        if "cursor" not in raw or raw["cursor"] is None:
            violations.append(Violation(path="cursor", reason="required"))
        else:
            cursor_value_raw = raw["cursor"]
            cursor_value_parsed = _parse_spec_integer(cursor_value_raw, "cursor", violations)
            if cursor_value_parsed is not None:
                cursor_value = cursor_value_parsed

        timeout_seconds_value: float | None = None
        if "timeoutSeconds" in raw:
            timeout_seconds_value_raw = raw["timeoutSeconds"]
            if timeout_seconds_value_raw is None:
                violations.append(Violation(path="timeoutSeconds", reason="explicit null not allowed"))
            else:
                if not (not isinstance(timeout_seconds_value_raw, bool) and isinstance(timeout_seconds_value_raw, (int, float))):
                    violations.append(Violation(path="timeoutSeconds", reason="expected number"))
                else:
                    timeout_seconds_value = timeout_seconds_value_raw
                    if not (-1.7976931348623157e308 <= timeout_seconds_value_raw <= 1.7976931348623157e308):
                        violations.append(Violation(path="timeoutSeconds", reason=f"must be a finite number, got {timeout_seconds_value_raw}"))

        for key in raw:
            if key != "sessionId" and key != "cursor" and key != "timeoutSeconds":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return PollMessagesInput(
            session_id=session_id_value,
            cursor=cursor_value,
            timeout_seconds=timeout_seconds_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "PollMessagesInput") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        out["sessionId"] = value.session_id
        out["cursor"] = value.cursor
        if value.timeout_seconds is not None:
            if not (-1.7976931348623157e308 <= value.timeout_seconds <= 1.7976931348623157e308):
                violations.append(Violation(path="timeoutSeconds", reason=f"must be a finite number, got {value.timeout_seconds}"))
            out["timeoutSeconds"] = value.timeout_seconds
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_PollMessagesInputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class PollMessagesInput:

    session_id: str
    """Provider-prefixed session identifier"""

    cursor: int

    timeout_seconds: float | None = None
    """How long the WorkflowStream poll update waits for new events before returning empty"""


class _PollMessagesOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["PollMessagesOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["PollMessagesOutput"]
    ) -> "PollMessagesOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        items_value: list[StreamItem] = typing.cast("typing.Any", None)
        if "items" not in raw or raw["items"] is None:
            violations.append(Violation(path="items", reason="required"))
        else:
            items_value_raw = raw["items"]
            if not isinstance(items_value_raw, list):
                violations.append(Violation(path="items", reason="expected array"))
            else:
                items_value_list: list[StreamItem] = []
                for items_value_index, items_value_element in enumerate(typing.cast("list[typing.Any]", items_value_raw)):
                    items_value_item_path = f'items[{items_value_index}]'
                    items_value_item_violation_count = len(violations)
                    items_value_item: StreamItem = typing.cast("typing.Any", None)
                    try:
                        items_value_item = _StreamItemTransferTypeConverter().from_transfer_type(items_value_element, StreamItem)
                    except ValidationError as error:
                        _collect(violations, items_value_item_path, error)
                    if len(violations) == items_value_item_violation_count:
                        items_value_list.append(items_value_item)
                items_value = items_value_list

        next_offset_value: int = typing.cast("typing.Any", None)
        if "next_offset" not in raw or raw["next_offset"] is None:
            violations.append(Violation(path="next_offset", reason="required"))
        else:
            next_offset_value_raw = raw["next_offset"]
            next_offset_value_parsed = _parse_spec_integer(next_offset_value_raw, "next_offset", violations)
            if next_offset_value_parsed is not None:
                next_offset_value = next_offset_value_parsed

        more_ready_value: bool = typing.cast("typing.Any", None)
        if "more_ready" not in raw or raw["more_ready"] is None:
            violations.append(Violation(path="more_ready", reason="required"))
        else:
            more_ready_value_raw = raw["more_ready"]
            if not isinstance(more_ready_value_raw, bool):
                violations.append(Violation(path="more_ready", reason="expected boolean"))
            else:
                more_ready_value = more_ready_value_raw

        closed_value: bool | None = None
        if "closed" in raw:
            closed_value_raw = raw["closed"]
            if closed_value_raw is None:
                violations.append(Violation(path="closed", reason="explicit null not allowed"))
            else:
                if not isinstance(closed_value_raw, bool):
                    violations.append(Violation(path="closed", reason="expected boolean"))
                else:
                    closed_value = closed_value_raw

        for key in raw:
            if key != "items" and key != "next_offset" and key != "more_ready" and key != "closed":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return PollMessagesOutput(
            items=items_value,
            next_offset=next_offset_value,
            more_ready=more_ready_value,
            closed=closed_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "PollMessagesOutput") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        items_out: list[typing.Any] = []
        for items_index, items_element in enumerate(value.items):
            try:
                items_out.append(_StreamItemTransferTypeConverter().to_transfer_type(items_element))
            except ValidationError as error:
                _collect(violations, f'items[{items_index}]', error)
        out["items"] = items_out
        out["next_offset"] = value.next_offset
        out["more_ready"] = value.more_ready
        if value.closed is not None:
            out["closed"] = value.closed
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_PollMessagesOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class PollMessagesOutput:
    """Mirrors WorkflowStream PollResult wire format so the async update-with-callback
    payload decodes correctly without transformation.
    """

    items: list[StreamItem]
    """Stream events since cursor; decode each as TurnEvent and map to Slack output"""

    next_offset: int
    """Next cursor value to use in the following pollMessages call"""

    more_ready: bool
    """True when more items are immediately available (batch was capped)"""

    closed: bool | None = None
    """True only in the sync error path when the agent workflow has already completed"""


class _ProvideCallbackResultInputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ProvideCallbackResultInput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ProvideCallbackResultInput"]
    ) -> "ProvideCallbackResultInput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        session_id_value: str = typing.cast("typing.Any", None)
        if "sessionId" not in raw or raw["sessionId"] is None:
            violations.append(Violation(path="sessionId", reason="required"))
        else:
            session_id_value_raw = raw["sessionId"]
            if not isinstance(session_id_value_raw, str):
                violations.append(Violation(path="sessionId", reason="expected string"))
            else:
                session_id_value = session_id_value_raw

        tool_id_value: str = typing.cast("typing.Any", None)
        if "toolId" not in raw or raw["toolId"] is None:
            violations.append(Violation(path="toolId", reason="required"))
        else:
            tool_id_value_raw = raw["toolId"]
            if not isinstance(tool_id_value_raw, str):
                violations.append(Violation(path="toolId", reason="expected string"))
            else:
                tool_id_value = tool_id_value_raw

        result_value: ProvideCallbackResultInputResult | None = None
        if "result" in raw:
            result_value_raw = raw["result"]
            if result_value_raw is None:
                violations.append(Violation(path="result", reason="explicit null not allowed"))
            else:
                try:
                    result_value = _ProvideCallbackResultInputResultTransferTypeConverter().from_transfer_type(result_value_raw, ProvideCallbackResultInputResult)
                except ValidationError as error:
                    _collect(violations, "result", error)

        error_value: str | None = None
        if "error" in raw:
            error_value_raw = raw["error"]
            if error_value_raw is None:
                violations.append(Violation(path="error", reason="explicit null not allowed"))
            else:
                if not isinstance(error_value_raw, str):
                    violations.append(Violation(path="error", reason="expected string"))
                else:
                    error_value = error_value_raw

        for key in raw:
            if key != "sessionId" and key != "toolId" and key != "result" and key != "error":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ProvideCallbackResultInput(
            session_id=session_id_value,
            tool_id=tool_id_value,
            result=result_value,
            error=error_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ProvideCallbackResultInput") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        out["sessionId"] = value.session_id
        out["toolId"] = value.tool_id
        if value.result is not None:
            try:
                out["result"] = _ProvideCallbackResultInputResultTransferTypeConverter().to_transfer_type(value.result)
            except ValidationError as error:
                _collect(violations, "result", error)
        if value.error is not None:
            out["error"] = value.error
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_ProvideCallbackResultInputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ProvideCallbackResultInput:
    """Payload for the provideCallbackResult operation. Exactly one of result/error should
    be set.
    """

    session_id: str
    """Provider-prefixed session identifier"""

    tool_id: str
    """The tool_id from the CallbackRequested stream event"""

    result: ProvideCallbackResultInputResult | None = None

    error: str | None = None
    """Set instead of result when the client could not fulfill the call"""


class _ProvideCallbackResultInputResultTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ProvideCallbackResultInputResult", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ProvideCallbackResultInputResult"]
    ) -> "ProvideCallbackResultInputResult":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)
        additional_properties: dict[str, typing.Any] = {}
        for key in raw:
            additional_properties[key] = raw[key]
        if violations:
            raise ValidationError(violations)
        return ProvideCallbackResultInputResult(additional_properties=additional_properties)

    @typing_extensions.override
    def to_transfer_type(self, value: "ProvideCallbackResultInputResult") -> typing.Any:
        out: dict[str, typing.Any] = {}
        for key, entry in value.additional_properties.items():
            out[key] = entry
        return out


@_transfer_type_convertible(_ProvideCallbackResultInputResultTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ProvideCallbackResultInputResult:
    """The value the client produced. Object-shaped only over this operation - nex-gen's Go
    target can't compile a oneOf with an object branch (generates a dangling type
    reference), so a scalar/list result must be wrapped, e.g. {"value": ...}.
    """

    additional_properties: dict[str, typing.Any] = dataclasses.field(default_factory=dict)


class _ProvideCallbackResultOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["ProvideCallbackResultOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["ProvideCallbackResultOutput"]
    ) -> "ProvideCallbackResultOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        tool_id_value: str = typing.cast("typing.Any", None)
        if "toolId" not in raw or raw["toolId"] is None:
            violations.append(Violation(path="toolId", reason="required"))
        else:
            tool_id_value_raw = raw["toolId"]
            if not isinstance(tool_id_value_raw, str):
                violations.append(Violation(path="toolId", reason="expected string"))
            else:
                tool_id_value = tool_id_value_raw

        accepted_value: bool = typing.cast("typing.Any", None)
        if "accepted" not in raw or raw["accepted"] is None:
            violations.append(Violation(path="accepted", reason="required"))
        else:
            accepted_value_raw = raw["accepted"]
            if not isinstance(accepted_value_raw, bool):
                violations.append(Violation(path="accepted", reason="expected boolean"))
            else:
                accepted_value = accepted_value_raw

        for key in raw:
            if key != "toolId" and key != "accepted":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return ProvideCallbackResultOutput(
            tool_id=tool_id_value,
            accepted=accepted_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "ProvideCallbackResultOutput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["toolId"] = value.tool_id
        out["accepted"] = value.accepted
        return out


@_transfer_type_convertible(_ProvideCallbackResultOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class ProvideCallbackResultOutput:

    tool_id: str
    """Echo of the resolved tool ID"""

    accepted: bool
    """Always true on success (the update validator rejects invalid submissions before the
    handler runs)
    """


class _QueryOperatorInterfaceOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["QueryOperatorInterfaceOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["QueryOperatorInterfaceOutput"]
    ) -> "QueryOperatorInterfaceOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        commands_value: list[OperatorCommand] = typing.cast("typing.Any", None)
        if "commands" not in raw or raw["commands"] is None:
            violations.append(Violation(path="commands", reason="required"))
        else:
            commands_value_raw = raw["commands"]
            if not isinstance(commands_value_raw, list):
                violations.append(Violation(path="commands", reason="expected array"))
            else:
                commands_value_list: list[OperatorCommand] = []
                for commands_value_index, commands_value_element in enumerate(typing.cast("list[typing.Any]", commands_value_raw)):
                    commands_value_item_path = f'commands[{commands_value_index}]'
                    commands_value_item_violation_count = len(violations)
                    commands_value_item: OperatorCommand = typing.cast("typing.Any", None)
                    try:
                        commands_value_item = _OperatorCommandTransferTypeConverter().from_transfer_type(commands_value_element, OperatorCommand)
                    except ValidationError as error:
                        _collect(violations, commands_value_item_path, error)
                    if len(violations) == commands_value_item_violation_count:
                        commands_value_list.append(commands_value_item)
                commands_value = commands_value_list

        for key in raw:
            if key != "commands":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return QueryOperatorInterfaceOutput(
            commands=commands_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "QueryOperatorInterfaceOutput") -> typing.Any:
        violations: list[Violation] = []
        out: dict[str, typing.Any] = {}
        commands_out: list[typing.Any] = []
        for commands_index, commands_element in enumerate(value.commands):
            try:
                commands_out.append(_OperatorCommandTransferTypeConverter().to_transfer_type(commands_element))
            except ValidationError as error:
                _collect(violations, f'commands[{commands_index}]', error)
        out["commands"] = commands_out
        if violations:
            raise ValidationError(violations)
        return out


@_transfer_type_convertible(_QueryOperatorInterfaceOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class QueryOperatorInterfaceOutput:

    commands: list[OperatorCommand]
    """Ordered list of operator commands the agent accepts"""


class _QuerySessionInputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["QuerySessionInput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["QuerySessionInput"]
    ) -> "QuerySessionInput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        session_id_value: str = typing.cast("typing.Any", None)
        if "sessionId" not in raw or raw["sessionId"] is None:
            violations.append(Violation(path="sessionId", reason="required"))
        else:
            session_id_value_raw = raw["sessionId"]
            if not isinstance(session_id_value_raw, str):
                violations.append(Violation(path="sessionId", reason="expected string"))
            else:
                session_id_value = session_id_value_raw

        for key in raw:
            if key != "sessionId":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return QuerySessionInput(
            session_id=session_id_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "QuerySessionInput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["sessionId"] = value.session_id
        return out


@_transfer_type_convertible(_QuerySessionInputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class QuerySessionInput:

    session_id: str
    """Provider-prefixed session identifier"""


class _SendAgentMessageInputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["SendAgentMessageInput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["SendAgentMessageInput"]
    ) -> "SendAgentMessageInput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        session_id_value: str = typing.cast("typing.Any", None)
        if "sessionId" not in raw or raw["sessionId"] is None:
            violations.append(Violation(path="sessionId", reason="required"))
        else:
            session_id_value_raw = raw["sessionId"]
            if not isinstance(session_id_value_raw, str):
                violations.append(Violation(path="sessionId", reason="expected string"))
            else:
                session_id_value = session_id_value_raw

        msg_type_value: str = typing.cast("typing.Any", None)
        if "msgType" not in raw or raw["msgType"] is None:
            violations.append(Violation(path="msgType", reason="required"))
        else:
            msg_type_value_raw = raw["msgType"]
            if not isinstance(msg_type_value_raw, str):
                violations.append(Violation(path="msgType", reason="expected string"))
            else:
                msg_type_value = msg_type_value_raw

        payload_value: str = typing.cast("typing.Any", None)
        if "payload" not in raw or raw["payload"] is None:
            violations.append(Violation(path="payload", reason="required"))
        else:
            payload_value_raw = raw["payload"]
            if not isinstance(payload_value_raw, str):
                violations.append(Violation(path="payload", reason="expected string"))
            else:
                payload_value = payload_value_raw

        expected_turn_value: int | None = None
        if "expectedTurn" in raw:
            expected_turn_value_raw = raw["expectedTurn"]
            if expected_turn_value_raw is None:
                violations.append(Violation(path="expectedTurn", reason="explicit null not allowed"))
            else:
                expected_turn_value_parsed = _parse_spec_integer(expected_turn_value_raw, "expectedTurn", violations)
                if expected_turn_value_parsed is not None:
                    expected_turn_value = expected_turn_value_parsed

        for key in raw:
            if key != "sessionId" and key != "msgType" and key != "payload" and key != "expectedTurn":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return SendAgentMessageInput(
            session_id=session_id_value,
            msg_type=msg_type_value,
            payload=payload_value,
            expected_turn=expected_turn_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "SendAgentMessageInput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["sessionId"] = value.session_id
        out["msgType"] = value.msg_type
        out["payload"] = value.payload
        if value.expected_turn is not None:
            out["expectedTurn"] = value.expected_turn
        return out


@_transfer_type_convertible(_SendAgentMessageInputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class SendAgentMessageInput:

    session_id: str
    """Provider-prefixed session identifier (e.g. slack:C12345)"""

    msg_type: str
    """Target @agent.accepts handler name (e.g. 'ask', 'slash')"""

    payload: str
    """JSON-encoded input matching the target handler's input model (e.g.
    '{"text":"hello"}' for the 'ask' handler)
    """

    expected_turn: int | None = None
    """Caller-known next turn number. Set this when the caller already tracks turn state
    (e.g. a subagent-driving parent) - the send fails fast with StaleTurn on a mismatch
    instead of guessing. Omit to fall back to guess-and-retry (e.g. Slack, which has no
    local turn state).
    """


class _SendMessageOutputTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["SendMessageOutput", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["SendMessageOutput"]
    ) -> "SendMessageOutput":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        turn_number_value: int = typing.cast("typing.Any", None)
        if "turnNumber" not in raw or raw["turnNumber"] is None:
            violations.append(Violation(path="turnNumber", reason="required"))
        else:
            turn_number_value_raw = raw["turnNumber"]
            turn_number_value_parsed = _parse_spec_integer(turn_number_value_raw, "turnNumber", violations)
            if turn_number_value_parsed is not None:
                turn_number_value = turn_number_value_parsed

        turn_id_value: str = typing.cast("typing.Any", None)
        if "turnId" not in raw or raw["turnId"] is None:
            violations.append(Violation(path="turnId", reason="required"))
        else:
            turn_id_value_raw = raw["turnId"]
            if not isinstance(turn_id_value_raw, str):
                violations.append(Violation(path="turnId", reason="expected string"))
            else:
                turn_id_value = turn_id_value_raw

        stream_head_offset_value: int | None = None
        if "streamHeadOffset" in raw:
            stream_head_offset_value_raw = raw["streamHeadOffset"]
            if stream_head_offset_value_raw is None:
                violations.append(Violation(path="streamHeadOffset", reason="explicit null not allowed"))
            else:
                stream_head_offset_value_parsed = _parse_spec_integer(stream_head_offset_value_raw, "streamHeadOffset", violations)
                if stream_head_offset_value_parsed is not None:
                    stream_head_offset_value = stream_head_offset_value_parsed

        pending_value: bool | None = None
        if "pending" in raw:
            pending_value_raw = raw["pending"]
            if pending_value_raw is None:
                violations.append(Violation(path="pending", reason="explicit null not allowed"))
            else:
                if not isinstance(pending_value_raw, bool):
                    violations.append(Violation(path="pending", reason="expected boolean"))
                else:
                    pending_value = pending_value_raw

        for key in raw:
            if key != "turnNumber" and key != "turnId" and key != "streamHeadOffset" and key != "pending":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return SendMessageOutput(
            turn_number=turn_number_value,
            turn_id=turn_id_value,
            stream_head_offset=stream_head_offset_value,
            pending=pending_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "SendMessageOutput") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["turnNumber"] = value.turn_number
        out["turnId"] = value.turn_id
        if value.stream_head_offset is not None:
            out["streamHeadOffset"] = value.stream_head_offset
        if value.pending is not None:
            out["pending"] = value.pending
        return out


@_transfer_type_convertible(_SendMessageOutputTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class SendMessageOutput:

    turn_number: int
    """Monotonic turn counter for this session"""

    turn_id: str
    """Unique ID for this turn"""

    stream_head_offset: int | None = None
    """Stream log offset at message-accept time; start the first pollMessages call from
    this offset to skip prior-turn history
    """

    pending: bool | None = None
    """True if the message was queued behind an active turn rather than dispatched
    immediately
    """


class _StreamItemTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["StreamItem", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["StreamItem"]
    ) -> "StreamItem":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        topic_value: str = typing.cast("typing.Any", None)
        if "topic" not in raw or raw["topic"] is None:
            violations.append(Violation(path="topic", reason="required"))
        else:
            topic_value_raw = raw["topic"]
            if not isinstance(topic_value_raw, str):
                violations.append(Violation(path="topic", reason="expected string"))
            else:
                topic_value = topic_value_raw

        data_value: str = typing.cast("typing.Any", None)
        if "data" not in raw or raw["data"] is None:
            violations.append(Violation(path="data", reason="required"))
        else:
            data_value_raw = raw["data"]
            if not isinstance(data_value_raw, str):
                violations.append(Violation(path="data", reason="expected string"))
            else:
                data_value = data_value_raw

        offset_value: int = typing.cast("typing.Any", None)
        if "offset" not in raw or raw["offset"] is None:
            violations.append(Violation(path="offset", reason="required"))
        else:
            offset_value_raw = raw["offset"]
            offset_value_parsed = _parse_spec_integer(offset_value_raw, "offset", violations)
            if offset_value_parsed is not None:
                offset_value = offset_value_parsed

        for key in raw:
            if key != "topic" and key != "data" and key != "offset":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return StreamItem(
            topic=topic_value,
            data=data_value,
            offset=offset_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "StreamItem") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["topic"] = value.topic
        out["data"] = value.data
        out["offset"] = value.offset
        return out


@_transfer_type_convertible(_StreamItemTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class StreamItem:
    """One event from WorkflowStream._log. Data is base64(proto
    Payload{encoding:json/plain, data:TurnEvent JSON}).
    """

    topic: str
    """Stream topic name (turn_events)"""

    data: str
    """base64-encoded proto Payload containing a TurnEvent"""

    offset: int
    """Absolute position of this item in the stream"""


class _SubagentInfoTransferTypeConverter(
    temporalio.converter.TransferTypeConverter["SubagentInfo", typing.Any]
):
    @typing_extensions.override
    def from_transfer_type(
        self, value: typing.Any, type_hint: type["SubagentInfo"]
    ) -> "SubagentInfo":
        violations: list[Violation] = []
        if not isinstance(value, dict):
            raise ValidationError([Violation(path="", reason="expected object")])
        raw = typing.cast("dict[str, typing.Any]", value)

        subagent_id_value: str = typing.cast("typing.Any", None)
        if "subagentId" not in raw or raw["subagentId"] is None:
            violations.append(Violation(path="subagentId", reason="required"))
        else:
            subagent_id_value_raw = raw["subagentId"]
            if not isinstance(subagent_id_value_raw, str):
                violations.append(Violation(path="subagentId", reason="expected string"))
            else:
                subagent_id_value = subagent_id_value_raw

        agent_key_value: str = typing.cast("typing.Any", None)
        if "agentKey" not in raw or raw["agentKey"] is None:
            violations.append(Violation(path="agentKey", reason="required"))
        else:
            agent_key_value_raw = raw["agentKey"]
            if not isinstance(agent_key_value_raw, str):
                violations.append(Violation(path="agentKey", reason="expected string"))
            else:
                agent_key_value = agent_key_value_raw

        workflow_id_value: str = typing.cast("typing.Any", None)
        if "workflowId" not in raw or raw["workflowId"] is None:
            violations.append(Violation(path="workflowId", reason="required"))
        else:
            workflow_id_value_raw = raw["workflowId"]
            if not isinstance(workflow_id_value_raw, str):
                violations.append(Violation(path="workflowId", reason="expected string"))
            else:
                workflow_id_value = workflow_id_value_raw

        next_expected_turn_value: int = typing.cast("typing.Any", None)
        if "nextExpectedTurn" not in raw or raw["nextExpectedTurn"] is None:
            violations.append(Violation(path="nextExpectedTurn", reason="required"))
        else:
            next_expected_turn_value_raw = raw["nextExpectedTurn"]
            next_expected_turn_value_parsed = _parse_spec_integer(next_expected_turn_value_raw, "nextExpectedTurn", violations)
            if next_expected_turn_value_parsed is not None:
                next_expected_turn_value = next_expected_turn_value_parsed

        for key in raw:
            if key != "subagentId" and key != "agentKey" and key != "workflowId" and key != "nextExpectedTurn":
                violations.append(Violation(path=key, reason="unknown field"))
        if violations:
            raise ValidationError(violations)
        return SubagentInfo(
            subagent_id=subagent_id_value,
            agent_key=agent_key_value,
            workflow_id=workflow_id_value,
            next_expected_turn=next_expected_turn_value,
        )

    @typing_extensions.override
    def to_transfer_type(self, value: "SubagentInfo") -> typing.Any:
        out: dict[str, typing.Any] = {}
        out["subagentId"] = value.subagent_id
        out["agentKey"] = value.agent_key
        out["workflowId"] = value.workflow_id
        out["nextExpectedTurn"] = value.next_expected_turn
        return out


@_transfer_type_convertible(_SubagentInfoTransferTypeConverter)
@dataclasses.dataclass(slots=True, kw_only=True)
class SubagentInfo:
    """An active subagent this agent is driving (mirrors harness SubagentInfo)"""

    subagent_id: str
    """The short id this agent references the subagent by"""

    agent_key: str

    workflow_id: str
    """The subagent's real child workflow ID"""

    next_expected_turn: int
