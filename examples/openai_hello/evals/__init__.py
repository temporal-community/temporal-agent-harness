"""Evals for the OpenAI Hello agent — the cross-SDK counterpart to examples/monty/evals.

Same dataset format, same runner, same scorer shape, different AI SDK. The only agent-specific
parts are the cases and what counts as doing the job well.
"""
