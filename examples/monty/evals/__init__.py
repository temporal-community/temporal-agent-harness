"""Evals for the Monty travel agent: the dataset, the scorers, and the runner.

The reusable machinery lives in ``temporal_agent_harness.evals``; what is here is the part that
is inherently specific to one agent's job — the cases, and what counts as doing that job well.
"""
